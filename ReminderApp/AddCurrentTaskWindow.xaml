<Window x:Class="ReminderApp.AddCurrentTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReminderApp"
        mc:Ignorable="d"
        Title="AddCurrentTaskWindow" Height="200" Width="400">

    <DockPanel>
        <Grid DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" VerticalContentAlignment="Center">Task</Label>
            <Label Grid.Row="1" Grid.Column="0" VerticalContentAlignment="Center">Date (eg. 18.08.2018 07:22:16)</Label>

            <TextBox Grid.Row="0" Grid.Column="1" x:Name="TaskBox"></TextBox>
            <TextBox Grid.Row="1" Grid.Column="1" x:Name="Date_TimeBox"></TextBox>

            <Button Grid.ColumnSpan="2" Grid.Row="2" Height="35" Width="100" Click="Finish_OnClick">Finish</Button>
        </Grid>
    </DockPanel>
</Window>
